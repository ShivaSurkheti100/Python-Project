## these 3 modules(packages) are not found installed
# import flask
# import pandas as pd  
# import pygame

# we can use independent python environment using virtual environment
'''# to use virutal environmetn install the package using syntax --> pip install virtualenv --< '''
'''We can create a new environment using: < python -m venv myprojectenv >'''
# for windows powershell users ; .\myprojectenv\Scripts\activate.ps1 ## enter S , a, p ani enter tab ; thenafter aafai aauxa
# enter a then press tab ; activate aafai aauxa. texpaxi (type .) ani press tab many times ; tespaxi ps1, fish, ...aru aafai aauxa
# didn't work 

# (myprojectenv -- virtual environment -- is deactivated using <deactivate>
# aaba deactivate garye paxi ; system interpreter chalxa k not myprojectenv
# pip install pygame is used to install pygame(package) ; mero ma myprojectenv(virtualenv) activate vayena due to error ; so Yo system interpreter ma matra install hunxa

''' in virtual env or system interpreter env; use <pip freeze> in the terminal to print all the packages(modules) installed in any environment along  with  the  versions'''

#### i wasnot able to do this due to bugs in windows ;;;; virtual environment ko ; I haven't done; due to bugs 
